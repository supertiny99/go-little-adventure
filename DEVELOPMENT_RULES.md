# 项目开发规范 (Development Rules)

> **生效日期**：2026-09-21  
> **核心原则**：本项目所有的围棋关卡与棋型必须严格对齐权威开源题库数据。

---

## 1. 题库对齐原则 (Open Source Puzzle Alignment)

为了杜绝人工主观摆放棋子带来的死子、逻辑漏洞或不正规争议，制定本规则：

1. **对齐开源标准**：
   - 基础吃子手筋题（门吃、抱吃、双打吃、倒扑、枷吃、接不归等）**必须直接采用经过权威引擎验证的开源题库坐标**（如 `geekhuashan/go-coach` 开源题库数据、101 围棋标准初学者题库、GoProblems 等）。
   - 禁止凭空随意摆设未经验证的棋型。
2. **源码注释与追溯**：
   - 在 `src/data/levels.ts` 中定义每一个关卡时，必须附带注释说明对应的开源题库编号或标准手筋原型（例如：`Source: go-coach tactic-gate`）。
3. **零死子原则**：
   - 任何关卡的初始棋盘上，绝不允许存在 0 气的死子，所有棋块气数必须 $\ge 1$。
4. **自动化测试守卫**：
   - 所有题目的正解走法、分支应手、提子判定，必须全部纳入 `src/engine/verifyLogic.ts` 测试套件。
   - 每次代码提交前，必须运行 `npx tsx src/engine/verifyLogic.ts` 且 100% 通过。
