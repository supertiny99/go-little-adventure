# 围棋小萌主开发规则 (Development Rules)

## 核心法则：题目必须对齐权威开源题库

为了杜绝人工主观摆子导致的死子、歧义或非正规棋形，本项目制定以下开发硬性规则（Rule）：

### 1. 题库来源规范 (Source of Truth)
- 本项目中所有关卡、谜题和实战题目，**必须对齐业界公认的权威开源围棋题库**（如 `geekhuashan/go-coach` 开源题库数据、101围棋少儿入门标准题库、GoProblems 标准题库等）。
- 每道题在代码（如 `src/data/levels.ts`）中定义时，必须明确标注其开源出处或标准手筋原型（例如 `tactic-gate`、`tactic-hug`、`tactic-double-atari`、`tactic-snapback`、`tactic-net` 等）。

### 2. 棋规与合法性底线 (Legal & Fair Go Rules)
- **绝对禁止 0 气死子**：初始盘面上任何现存棋子（单子或连通块）的气数必须 $\ge 1$，绝不允许开局摆放无气死子。
- **正解唯一与分支严谨**：吃子手筋与死活题目必须经过引擎程序与正反手推演（每一步必须符合逻辑，对方逃跑与我方追击符合围棋正理）。
- **自动化测试强制校验**：任何新增或修改的关卡，必须在 `src/engine/verifyLogic.ts` 中编写端到端单元测试，执行 `npx tsx src/engine/verifyLogic.ts` 必须 100% 校验通过方可合并。

### 3. 儿童认知与语言适龄化 (Kid-Friendly Adaptation)
- **底层严谨，表层童趣**：棋形底座严格采用开源标准坐标，上层展示则使用 5 岁儿童易于理解的具象化隐喻（如“关门大门”、“大拥抱”、“小草帽/神奇大网”、“踩进陷阱反提”）。
- **语音与微动效协同**：每道题需配套 Web Speech API 朗读文本及濒危（1气冒汗）视觉反馈。
