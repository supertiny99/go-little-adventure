# 围棋题目开发规范

## 所有的题目必须对齐权威开源题库

1. **题库来源与标注**：
   所有关卡和棋题必须采用开源权威题库（如 `geekhuashan/go-coach`、101围棋等）的标准数据，严禁人工随意摆子。在代码中必须保留开源题目 ID 或对应手筋标准分类。

2. **初始状态合法性**：
   初始盘面不得存在 0 气死子，所有棋块气数 >= 1。

3. **测试覆盖**：
   每次变更必须运行并通过 `npx tsx src/engine/verifyLogic.ts` 自动化推演。
